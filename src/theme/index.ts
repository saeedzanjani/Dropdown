// Components & Files
import { palette, radius, shadow, fontSize, width } from "./components";
import { ITheme } from "../DTOs/interfaces/theme.model";

// Theme
export const theme: ITheme = {
  palette,
  radius,
  shadow,
  fontSize,
  width,
};

// Packages
import {createUseStyles} from 'react-jss'


// Styles Start
export const useStyles = createUseStyles({
    root: {
        width: '100%',
        height: '100vh',
        margin: '0 auto',
        overflow: 'hidden'
    }
})
// Styles End